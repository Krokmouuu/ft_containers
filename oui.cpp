#include "vector.hpp"
#include "vector"

int main()
{
	ft::vector<int> oui;
    ft::vector<int> non (10, 20);
    // std::vector<int> cc (10, 20);


    // for (size_t i = 0; i < 10; i++)
    //     cout << non[0] << endl;

	return 0;
}